# Tasks

**Proyecto**: [[projects/guardian-live-monitor/README|guardian-live-monitor]]


- **M0: Infraestructura base (Docker Compose, .env, .gitignore)** [pending]: Crear docker-compose.yml con servicios postgres:16-alpine, redis:7-alpine, backend (build ./backend), detector (build ./detector), dashboard (build ./dashboard). Red bridge compartida guardian-net. Volumen persistente pgdata para PostgreSQL. Configurar .env.example con DATABASE_URL, REDIS_URL, BACKEND_URL, CAMERA_ID, SOURCE (webcam|video). .gitignore para Python, Docker, .env.
- **M1.1: Backend Core — Config, Modelos Pydantic, DB y ORM** [pending]: Crear estructura backend/app/ con: config.py (pydantic-settings: DATABASE_URL, REDIS_URL, CORS origins), models.py (EventPayload: camera_id, event_type, severity, confidence), database.py (async SQLAlchemy engine + session factory con asyncpg), schemas.py (ORM Event: id UUID, camera_id str, event_type str, severity str, confidence float, timestamp TIMESTAMPTZ). Requisitos.txt con: fastapi, uvicorn, pydantic, pydantic-settings, sqlalchemy[asyncio], asyncpg, redis[hiredis], httpx.
- **M1.2: Backend Core — Router, Redis Pub/Sub, WebSocket Manager y Main** [pending]: Crear backend/app/routers/events.py (POST /api/events: valida con Pydantic, inserta en PostgreSQL, publica en Redis canal live_events, responde 201). redis_client.py (conexión asíncrona Redis, método publish_event). ws_manager.py (ConnectionManager: lista de WebSocket activos, connect/disconnect, broadcast). main.py (FastAPI app: startup crea tablas + inicia worker Redis->WebSocket, shutdown limpia, incluye router, endpoint WS /ws/events). Worker: bucle asyncio escuchando Redis Pub/Sub que llama ws_manager.broadcast(). Crear backend/Dockerfile (python:3.12-slim, instala requirements, expone 8000, CMD uvicorn).
- **M2: Detector de Movimiento (OpenCV)** [pending]: Crear detector/detector.py: acceso a webcam (cv2.VideoCapture(0)) o video de prueba (configurable por env SOURCE=webcam|video y VIDEO_PATH). Frame differencing con cv2.absdiff y threshold para detectar movimiento. Si píxeles en movimiento > umbral, dispara POST asíncrono con httpx a BACKEND_URL/api/events. Cooldown de 3s entre eventos. Payload: camera_id (env CAMERA_ID), event_type=motion_detected, severity (low/medium/high según confidence), confidence (0.0-1.0). Loop principal con fps controlado. Crear detector/Dockerfile (python:3.12-slim, instala opencv-python, httpx, numpy, expone nada, CMD python detector.py). requirements.txt.
- **M3: Dashboard Web (HTML + Tailwind + JS)** [pending]: Crear dashboard/public/index.html con: layout responsivo usando Tailwind CSS vía CDN. Header con título Guardian Live Monitor. Sección de analítica: total alertas en sesión, última severidad, estado conexión (Online/Offline). Feed de eventos en vivo con tarjetas que muestran: camera_id, event_type, severity (color-coded: low=green, medium=yellow, high=red), confidence (barra de progreso), timestamp. app.js: conexión WebSocket a ws://localhost:8000/ws/events con reconexión automática (exponential backoff 1s-30s). Renderizado dinámico: nuevos eventos se insertan al inicio del feed con transición CSS fade-in/slide-in. Contador de evento incrementa. Actualiza estado conexión. Crear dashboard/nginx.conf (servir /public, proxy reverso no necesario). Dockerfile (nginx:alpine, copiar nginx.conf y public/).